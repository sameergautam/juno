version: 0.2

phases:
  build:
    commands:
      - echo "Installer build started on `date`"
      - npm install
      - npm run compile-win
  post_build:
    commands:
      - echo "Pushing artifacts"
      - aws s3 ls 
artifacts:
  type: zip
  files:
     - 'dist/*'
  discard-paths: yes